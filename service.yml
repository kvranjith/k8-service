kind: Service
apiVersion: v1
metadata:
  name: nginx-svc
  namespace: elk
spec:
  selector:
    app: nginx
    tier: backend
  ports:
  - protocol: TCP
    port: 80
    targetPort: http
  type: LoadBalancer